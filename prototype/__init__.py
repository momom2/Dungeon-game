"""Beta-testing prototype — stripped-down playable demo.

Imports shared infrastructure from ``dungeon_builder/`` but replaces
game logic with simplified mechanics: stone-only terrain, two crafting
recipes (arrow trap + door), auto-spawning explorer intruders, no
physics.

Run with: ``python -m prototype``
"""
